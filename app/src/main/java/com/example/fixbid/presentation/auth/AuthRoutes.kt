package com.example.fixbid.presentation.auth

object AuthRoutes {
    const val Welcome = "welcome"
    const val Login = "login"
    const val Register = "register"
    const val Otp = "otp"
    const val ForgotPassword = "forgot_password"
    /** Shown right after a successful OAuth sign-in if the profile row doesn't exist yet. */
    const val OAuthRolePicker = "oauth_role_picker"
    const val Home = "home"
}
